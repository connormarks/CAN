This custom package was created by Nathan Pietrantonio for use in:
    Baselines/FewShotLLM
    Baselines/LogisticRegression
    SyntheticDataGeneration

A custom package to contain the local and Gemini LLM generation logic.
This package also contains some logic for displaying synthetic (LLM) dataset results in our Non-LLM baesline

Install using:
`pip install -e <path to /shared>`
