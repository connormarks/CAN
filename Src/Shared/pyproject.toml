[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "custom_llm_tools"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "google-genai",
    "python-dotenv",
    "ollama",
]

[tool.setuptools.packages.find]
where = ["."]
